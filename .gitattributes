# GitHub (Linguist) has no Telic grammar yet; Forth's is the closest fit,
# so .telic files render with Forth highlighting on github.com. Replace with
# a proper Linguist submission once usage clears their bar (see PLAN).
*.telic linguist-language=Forth
